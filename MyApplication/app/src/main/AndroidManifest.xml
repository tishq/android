<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" /> <!-- 使用网络的权限 -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/aa"
        android:label="@string/app_name"
        android:roundIcon="@drawable/aa"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ArticleRecycleActivity"></activity>
        <activity android:name=".test.BMBActivity"></activity>
        <activity android:name=".WebActivity" />
        <activity android:name=".test.RecyclerViewActivity"></activity>
        <activity android:name=".test.ListViewActivity" />
        <activity android:name=".MoreMegActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>